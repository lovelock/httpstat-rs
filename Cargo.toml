[package]
name = "httpstat"
version = "0.1.0"
edition = "2021"
description = "curl statistics made simple — Rust rewrite"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
